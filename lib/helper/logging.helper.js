const logger = {
  // eslint-disable-next-line no-console
  warn: (...arg) => console.warn(...arg),
  // eslint-disable-next-line no-console
  error: (...arg) => console.error(...arg),
};

module.exports = {
  logger,
};
